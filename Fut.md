# 整套业务
房东端在每次租客退房后都要更新房源的照片才能重新出租，更新房子的图片不会带着更新评论，只是把图片进行更新
用户租房前需要签订电子合同(PDF)，线下可以签订纸质合同(不在我们业务范围内)
后续要使用OCR技术(PDF内容审核)，对合同进行审查，确保合同内容正确合法，保证租客和房东的权益
签订的合同需要保存到minio存储中，单独开一个数据库和bucket存储合同文件，并且记录时间，时间包括签订时间和退房时间
做一个定时任务类，每天扫描一次
当退房后超过合同的生效时间，会用定时任务类进行检查，然后删除，防止大量合同文件占用存储空间
# 后续添加功能
后续可以改成微信登录，用户使用微信小程序时，就可以直接登录
将管理员端的jwt令牌机制改成双token，一个短token，一个长token，短token用于每次请求，长token用于刷新短token
# 优化结构
将AI模块、微信聊天模块和minio的模块分类出来，放到不同的模块内
建立一个OCR模块
后续还可能引入tika做文档的解析
# 优化数据库
后续更新合同和微信登录，就要更新数据库和jwt令牌机制
# 优化模块
微信聊天模块改成使用Netty框架实现，将他放到一个独立的模块内进行管理


