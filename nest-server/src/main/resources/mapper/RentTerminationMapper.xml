<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nest.mapper.RentTerminationMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO rent_termination
        (order_id, tenant_id, apply_time, effective_end_period, refund_status, remark, create_time)
        VALUES (#{orderId}, #{tenantId}, NOW(), #{effectiveEndPeriod}, 0, #{remark}, NOW())
    </insert>

    <select id="selectByOrderId" resultType="RentTermination">
        SELECT * FROM rent_termination WHERE order_id = #{orderId} LIMIT 1
    </select>

    <select id="selectPendingRefund" resultType="RentTermination">
        SELECT * FROM rent_termination WHERE refund_status = 0
    </select>

    <update id="updateRefund">
        UPDATE rent_termination
        SET refund_status = #{refundStatus}, refund_time = #{refundTime}, refund_txn_id = #{refundTxnId}
        WHERE id = #{id}
    </update>

</mapper>
