<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nest.mapper.FavoriteMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO favorite (tenant_id, house_id, create_time)
        VALUES (#{tenantId}, #{houseId}, NOW())
    </insert>

    <delete id="deleteByTenantAndHouse">
        DELETE FROM favorite WHERE tenant_id = #{tenantId} AND house_id = #{houseId}
    </delete>

    <select id="selectByTenant" resultType="Favorite">
        SELECT * FROM favorite
        WHERE tenant_id = #{tenantId}
        ORDER BY create_time DESC
    </select>

    <select id="selectByTenantAndHouse" resultType="Favorite">
        SELECT * FROM favorite
        WHERE tenant_id = #{tenantId} AND house_id = #{houseId}
        LIMIT 1
    </select>

</mapper>
