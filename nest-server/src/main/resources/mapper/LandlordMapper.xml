<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nest.mapper.LandlordMapper">

    <select id="selectByPhone" resultType="Landlord">
        SELECT * FROM landlord WHERE phone = #{phone}
    </select>

    <select id="selectById" resultType="Landlord">
        SELECT * FROM landlord WHERE id = #{id}
    </select>

    <select id="selectByIds" resultType="Landlord">
        SELECT id, name, avatar FROM landlord
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO landlord (name, phone, password, status, create_time, update_time)
        VALUES (#{name}, #{phone}, #{password}, #{status}, NOW(), NOW())
    </insert>

    <update id="updatePassword">
        UPDATE landlord SET password = #{password} WHERE id = #{id}
    </update>

</mapper>
