<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nest.wallet.mapper.WalletTransactionMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO wallet_transaction (wallet_id, user_type, user_id, biz_type, amount,
            direction, balance_after, source, status, biz_no, idem_key, peer_txn_id, remark, create_time)
        VALUES (#{walletId}, #{userType}, #{userId}, #{bizType}, #{amount},
            #{direction}, #{balanceAfter}, #{source}, #{status}, #{bizNo}, #{idemKey}, #{peerTxnId}, #{remark}, NOW())
    </insert>

    <!-- 幂等快路径：命中即说明这笔提现已经受理过 -->
    <select id="selectByIdemKey" resultType="WalletTransaction">
        SELECT * FROM wallet_transaction WHERE idem_key = #{idemKey}
    </select>

    <update id="updatePeerTxn">
        UPDATE wallet_transaction SET peer_txn_id = #{peerTxnId} WHERE id = #{id}
    </update>

    <select id="selectByUser" resultType="WalletTransaction">
        SELECT * FROM wallet_transaction
        WHERE user_type = #{userType} AND user_id = #{userId}
        <if test="bizType != null and bizType != ''">
            AND biz_type = #{bizType}
        </if>
        ORDER BY create_time DESC, id DESC
    </select>

</mapper>
